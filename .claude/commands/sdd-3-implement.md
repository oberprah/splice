# SDD Phase 3: Implement

Start Phase 3 of the spec-driven development workflow.

## Arguments

$ARGUMENTS - Path to the spec folder (e.g., `docs/specs/241219-user-export`)

## Instructions

First, read `docs/workflows/spec-driven/00-overview.md` to understand the overall workflow.

Then, read and follow the phase instructions in `docs/workflows/spec-driven/03-implement.md`.

The feature folder is:

```
$ARGUMENTS
```

Read the approved design document and requirements from this folder before starting.
