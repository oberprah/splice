apiVersion: v1
kind: Service
metadata:
  name: api-proxy
  namespace: agent-env
spec:
  selector:
    app: api-proxy
  ports:
    - port: 8080
      targetPort: 8080
      name: anthropic
    - port: 8081
      targetPort: 8081
      name: openai
